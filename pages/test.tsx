import { combinations, partitions } from '../solver/combinations'

console.log(1, partitions(['a', 'b', 'c', 'd']))
console.log(2, combinations(['a', 'b', 'c', 'd'], 2))

export default function Named() {
  return 'hi'
}
